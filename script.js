const display = document.querySelector(".display");

window.onload = function() {
    const button = document.querySelector('.divided');
    button.style.transition = 'all 0.5s ease';
    button.style.transform = 'scale(1.2)';
}
